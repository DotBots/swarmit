[tox]
envlist = check,cli,dashboard-cli
skip_missing_interpreters = true
isolated_build = true

[testenv:.pkg]
passenv =
    SKIP_SDIST_HOOK

[testenv]
commands=
    check:          {[testenv:check]commands}
    cli:            {[testenv:cli]commands}
    dashboard-cli:  {[testenv:dashboard-cli]commands}
deps=
    {[testenv:check]deps}

[testenv:check]
deps=
    pre-commit
commands=
    pre-commit run --all-files --show-diff-on-failure

[testenv:cli]
commands=
    swarmit --help

[testenv:dashboard-cli]
extras=
    dashboard
commands=
    python3 -m swarmit.dashboard.main --help

[testenv:format]
deps=
    black
commands=
    black .
